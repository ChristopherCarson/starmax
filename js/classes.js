class Ship {
     constructor(ship_id, inv_id, name, make, model, ship_condition, long_description, cost, year_of_production, image_url, action_image_url) {
         this.ship_id = ship_id;
         this.inv_id = inv_id;
         this.name = name;
         this.make = make;
         this.model = model;
         this.ship_condition = ship_condition;
         this.long_description = long_description;
         this.cost = cost;
         this.year_of_production = year_of_production;
         this.image_url = image_url;
         this.action_image_url = action_image_url;
     }
}